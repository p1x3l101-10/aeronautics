#Enable debug logging for all mechanics (spams logs, use only for testing)
debug = false

[tweaks]
	#Always enable filter by hand option in the gun smith table
	alwaysFilterByHand = true
	#Suppresses the sound that plays when you land a headshot
	suppressHeadHitSounds = false
	#Suppresses the sound that plays when you land a shot that is not a headshot
	suppressFleshHitSounds = false
	#Suppresses the sound that plays when you kill an entity with a gun
	suppressKillSounds = false
	#Hides hit markers when hitting entities
	hideHitMarkers = false
	#Completely hides the gun crosshair (TACZ crosshair)
	hideGunCrosshair = false

[distantFire]
	#Enable distant fire sounds (hear gunshots from far away)
	enabled = true
	#Minimum distance (blocks) for distant fire effect to start
	# Default: 64
	# Range: 16 ~ 500
	minDistance = 64
	#Maximum distance (blocks) to hear distant fire
	# Default: 300
	# Range: 100 ~ 1000
	maxDistance = 300
	#Volume multiplier for distant fire sounds
	# Default: 0.7
	# Range: 0.1 ~ 1.0
	volumeMultiplier = 0.7

[whizz]
	#Enable bullet whizz sounds (hear bullets passing by)
	enabled = true

[suppression]
	#Enable suppression visual effect when bullets fly near or impact nearby
	enabled = true
	#Maximum distance (blocks) for suppression detection
	# Default: 10.0
	# Range: 1.0 ~ 50.0
	detectionRadius = 10.0
	#Base intensity added per bullet fly-by at closest range (0.0-1.0)
	# Default: 0.25
	# Range: 0.01 ~ 1.0
	flybyIntensity = 0.25
	#Intensity multiplier for bullet impacts near player (relative to fly-by)
	# Default: 0.7
	# Range: 0.1 ~ 2.0
	impactIntensityMultiplier = 0.7
	#Camera shake intensity multiplier (0.0-3.0)
	# Default: 1.0
	# Range: 0.0 ~ 3.0
	shakeIntensity = 1.0
	#Camera shake speed (frequency of shakes)
	# Default: 1.0
	# Range: 0.0 ~ 3.0
	shakeSpeed = 1.0
	#Suppression decay per tick (how fast the effect fades)
	# Default: 0.015
	# Range: 0.001 ~ 0.2
	decayRate = 0.015
	#Maximum suppression intensity (0.0-1.0)
	# Default: 1.0
	# Range: 0.1 ~ 1.0
	maxIntensity = 1.0
	#Blur effect strength multiplier
	# Default: 1.0
	# Range: 0.0 ~ 3.0
	blurStrength = 1.0
	#Vignette darkening strength multiplier
	# Default: 1.0
	# Range: 0.0 ~ 3.0
	vignetteStrength = 1.0
	#Color desaturation strength multiplier
	# Default: 1.0
	# Range: 0.0 ~ 3.0
	desaturationStrength = 1.0

[ricochet]
	#Enable bullet ricochet off blocks
	enabled = true
	#Enable demo ricochet preset (forces 100% ricochet-friendly values)
	demoPreset = false
	#Enable debug logging for ricochet decisions
	debug = false
	#Minimum bullet speed required to ricochet
	# Default: 1.0
	# Range: 0.05 ~ 100.0
	minSpeed = 1.0
	#Minimum incidence angle (degrees) from surface normal to allow ricochet
	# Default: 60.0
	# Range: 10.0 ~ 89.0
	minAngle = 60.0
	#Maximum number of ricochets per bullet
	# Default: 1
	# Range: 0 ~ 10
	maxBounces = 1
	#Speed multiplier applied after ricochet (real-world: ~35% loss = 0.65)
	# Default: 0.65
	# Range: 0.1 ~ 1.0
	speedMultiplier = 0.65
	#How much the reflection flattens along the surface (0=perfect mirror, 0.2=realistic)
	# Default: 0.15
	# Range: 0.0 ~ 0.5
	flattenReflection = 0.15
	#Chance of ricochet when conditions are met (0.0-1.0)
	# Default: 0.5
	# Range: 0.0 ~ 1.0
	chance = 0.5

[freeAim]
	#Enable free aim - gun direction lags behind camera movement
	enabled = true
	#Maximum angle (degrees) the gun can deviate from view direction
	# Default: 2.5
	# Range: 0.5 ~ 25.0
	maxAngle = 2.5
	#Speed at which the gun catches up to view direction (0.0-1.0, higher = faster)
	# Default: 0.15
	# Range: 0.01 ~ 1.0
	lerpSpeed = 0.15
	#Disable free aim when aiming down sights
	disableWhenAiming = true
	#Scale factor for converting free aim angle to screen pixels
	# Default: 10.0
	# Range: 1.0 ~ 50.0
	crosshairScale = 10.0
	#Disable crosshair movement with free aim (crosshair stays centered)
	disableCrosshairMovement = false

[movement]
	#Enable advanced movement mechanics (sitting, crawling, leaning, sliding)
	enabled = true
	#Enable sitting/crouching pose
	sitEnabled = true
	#Enable crawling/prone pose
	crawlEnabled = true
	#Enable sliding when sprinting + sit key
	slideEnabled = true
	#Maximum sliding force
	# Default: 1.0
	# Range: 0.1 ~ 3.0
	slideMaxForce = 1.0
	#Auto-hold lean position (toggle mode)
	leanAutoHold = false
	#Correct mouse input when leaning
	leanMouseCorrection = true
	#Limit view angle when crawling
	crawlBlockView = true
	#Maximum view angle when crawling (in degrees)
	# Default: 90.0
	# Range: 30.0 ~ 180.0
	crawlBlockAngle = 90.0
	#Cooldown between sit actions (seconds)
	# Default: 0.75
	# Range: 0.0 ~ 5.0
	sitCooldown = 0.75
	#Cooldown between crawl actions (seconds)
	# Default: 0.75
	# Range: 0.0 ~ 5.0
	crawlCooldown = 0.75
	#Cooldown between lean actions (seconds)
	# Default: 0.0
	# Range: 0.0 ~ 5.0
	leanCooldown = 0.0
	#Auto-hold sit position (toggle mode)
	sitAutoHold = true
	#Hitbox height when sitting/crouching
	# Default: 1.2
	# Range: 0.2 ~ 3.0
	sitHeight = 1.2
	#Hitbox width when sitting/crouching
	# Default: 0.6
	# Range: 0.1 ~ 3.0
	sitWidth = 0.6
	#Eye height when sitting/crouching
	# Default: 1.0
	# Range: 0.1 ~ 3.0
	sitEyeHeight = 1.0
	#Hitbox height when crawling
	# Default: 0.6
	# Range: 0.1 ~ 3.0
	crawlHeight = 0.6
	#Hitbox width when crawling
	# Default: 1.0
	# Range: 0.1 ~ 3.0
	crawlWidth = 1.0
	#Eye height when crawling
	# Default: 0.4
	# Range: 0.1 ~ 3.0
	crawlEyeHeight = 0.4
	#Vertical offset for sitting hitbox (positive = up, negative = down)
	# Default: 0.0
	# Range: -5.0 ~ 5.0
	sitYOffset = 0.0
	#Vertical offset for crawling hitbox (positive = up, negative = down)
	# Default: 0.0
	# Range: -5.0 ~ 5.0
	crawlYOffset = 0.0
